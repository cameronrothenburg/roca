pub fn show(n: Number) -> String {
            const msg = n.toString()
            log(msg)
            return msg
            crash {
                n.toString -> skip
                log -> skip
            }
            test { self(42) == "42" }
        }
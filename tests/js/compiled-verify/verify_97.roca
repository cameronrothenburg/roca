pub fn fail_all(x: Number) -> String, err {
            return match x {
                1 => err.a
                2 => err.b
                _ => err.c
            }
            test {
                self(1) is err.a
                self(2) is err.b
                self(99) is err.c
            }
        }
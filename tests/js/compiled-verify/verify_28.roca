pub fn only_big() -> String {
            const nums = [1, 5, 10, 15, 3]
            const big = nums.filter(fn(x) -> x > 5)
            return big.join(",")
            crash {
                nums.filter -> skip
                big.join -> skip
            }
            test { self() == "10,15" }
        }
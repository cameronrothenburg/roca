pub fn classify(x: Number) -> String {
            if x > 0 {
                if x > 100 { return "big" }
                return "small"
            }
            return "negative"
            test {
                self(200) == "big"
                self(5) == "small"
                self(-1) == "negative"
            }
        }
pub fn describe(code: Number) -> String {
            return match code {
                200 => "ok"
                404 => "not found"
                500 => "error"
                _ => "unknown"
            }
            test {
                self(200) == "ok"
                self(404) == "not found"
                self(999) == "unknown"
            }
        }
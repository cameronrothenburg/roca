{
  "name": "worker-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "roca build",
    "dev": "roca build && wrangler dev",
    "deploy": "roca build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  },
  "dependencies": {
    "roca-worker-lib": "file:out"
  }
}

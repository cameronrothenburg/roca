// Core contracts — what things must do

contract Stringable {
    to_string() -> String
}

contract Validatable {
    validate(raw: String) -> Self, err {
        err invalid = "validation failed"
    }
}

contract StatusCode {
    200
    400
    404
    500
}

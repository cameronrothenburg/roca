[package]
name = "roca-lang"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Roca language AST types — shared IR for all compiler backends"

[dependencies]

pub fn make_array() -> String {
            const arr = [1, 2, 3]
            const result = arr.map(fn(x) -> "item")
            return result.join(",")
            crash {
                arr.map -> skip
                result.join -> skip
            }
            test { self() == "item,item,item" }
        }
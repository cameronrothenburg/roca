pub fn repeat_str(s: String, n: Number) -> String {
            let result = ""
            let i = 0
            while i < n {
                result = result + s
                i = i + 1
            }
            return result
            test { self("ab", 3) == "ababab" }
        }
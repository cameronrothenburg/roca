pub fn shout() -> String {
            const words = ["hello", "world"]
            const upper = words.map(fn(w) -> w.toUpperCase())
            return upper.join(" ")
            crash {
                words.map -> skip
                upper.join -> skip
            }
            test { self() == "HELLO WORLD" }
        }
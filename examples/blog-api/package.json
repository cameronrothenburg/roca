{
  "name": "blog-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "roca build",
    "dev": "roca build && wrangler dev",
    "deploy": "roca build && wrangler deploy",
    "migrate": "wrangler d1 execute blog --local --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4"
  },
  "dependencies": {
    "blog-api": "file:out"
  }
}

pub fn roundtrip(s: String) -> String {
            const encoded = btoa(s)
            const decoded = atob(encoded)
            return decoded
            crash {
                btoa -> skip
                atob -> skip
            }
            test { self("hello") == "hello" }
        }
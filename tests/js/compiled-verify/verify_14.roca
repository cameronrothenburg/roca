pub fn sign(x: Number) -> String {
            if x > 0 {
                return "positive"
            } else {
                return "non-positive"
            }
            test {
                self(5) == "positive"
                self(-1) == "non-positive"
            }
        }
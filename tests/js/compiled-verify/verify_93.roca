contract Settable { set_name(n: String) -> String }

        pub struct User {
            name: String
        }{}

        User satisfies Settable {
            fn set_name(n: String) -> String {
                self.name = n
                return self.name
                test { self("cam") == "cam" }
            }
        }
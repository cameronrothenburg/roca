// Deep stdlib tests: Process contract

import { Process } from std::process

/// Test
pub fn test_cwd() -> Bool {
    const dir = Process.cwd()
    return dir.length > 0
    test {
        self() is Ok
    }
}

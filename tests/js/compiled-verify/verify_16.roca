pub fn clamp(val: Number, min: Number, max: Number) -> Number {
            if val < min { return min }
            if val > max { return max }
            return val
            test {
                self(5, 0, 10) == 5
                self(-5, 0, 10) == 0
                self(50, 0, 10) == 10
            }
        }
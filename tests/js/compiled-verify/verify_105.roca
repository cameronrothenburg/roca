pub fn count_to(n: Number) -> Number {
            let i = 0
            let total = 0
            while i < n {
                total = total + i
                i = i + 1
            }
            return total
            test { self(5) == 10 }
        }